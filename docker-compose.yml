version: '3.8'

services:
  zip-tool:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MAX_ARCHIVE_SIZE=5368709120  # 5GB
      - RATE_LIMIT_PER_MINUTE=10
    restart: unless-stopped

